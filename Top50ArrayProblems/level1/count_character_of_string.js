function countCharacter(array) {
    // Create an empty object to hold character count
    let charCount = {};

    // Loop through each character in the string
    for (let i = 0; i < array.length; i++) {
        const element = array[i];
        
    }

}